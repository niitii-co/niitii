# APP SERVER

#/etc/supervisor/conf.d/niitii.conf
    [program:niitii]
    command=/bin/sh -c "/home/niitii/niitii/venv/bin/gunicorn -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker -b 192.168.0.103:8000 -w 1 niitii:app & /home/niitii/niitii/venv/bin/gunicorn -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker -b 192.168.0.103:8001 -w 1 niitii:app & /home/niitii/niitii/venv/bin/gunicorn -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker -b 192.168.0.103:8002 -w 1 niitii:app & /home/niitii/niitii/venv/bin/gunicorn -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker -b 192.168.0.103:8003 -w 1 niitii:app & /home/niitii/niitii/venv/bin/gunicorn -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker -b 192.168.0.103:8004 -w 1 niitii:app"
    directory=/home/niitii/niitii
    user=niitii
    autostart=true
    autorestart=true
    stopasgroup=true
    killasgroup=true

# reload after updating config file
$ sudo supervisorctl reload
    
# restart app
$ sudo supervisorctl restart all


# NGINX Server

#/etc/nginx/sites-available/niitii
# Define which servers to include in the load balancing scheme. 
# private IPs for better performance and security.
# each port matches a gunicorn process
   upstream backend {
      # session persistence, alt to ip_hash. $remote_addr will hash to all four octets 
      hash $remote_addr;
      server 192.168.0.103:8000;
      server 192.168.0.103:8001;
      server 192.168.0.103:8002;
      server 192.168.0.103:8003;
      server 192.168.0.103:8004; 
   }

   # This server accepts all traffic to port 80 and passes it to the upstream. 
   server {
       # listen on port 80 (http)
       listen 80;
       listen [::]:80;
       # catch all
       server_name _;
       location / {
            # redirect any requests to the same URL but on https
            return 301 https://$host$request_uri;
        }
    }

   # upstream name and the proxy_pass need to match.
    server {
        # listen on port 443 (https)
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name _;          # catch all
  
        # location of the self-signed SSL certificate
        ssl_certificate /home/niitii/certs/cert.pem;
        ssl_certificate_key /home/niitii/certs/key.pem;

        # write access and error logs to /var/log
        access_log /var/log/niitii_access.log;
        error_log /var/log/niitii_error.log;
        
        location / {
            # from "include proxy_params;"
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # forward application requests to the gunicorn server
            #proxy_pass http://localhost:8000;
            proxy_pass http://backend;
            proxy_redirect off;
        }

        location /socket.io {
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_http_version 1.1;
            proxy_buffering off;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_pass http://backend/socket.io;
        }
    }


# VALID DOMAINS
Valid domains for niitii: (.net, .app, .com, .info, .org)
The domain service provider is IONOS
